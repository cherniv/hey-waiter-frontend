{
  "name": "hey-waiter-frontend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --prefix manager ; mv manager/public/ public/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mobx": "^5.14.2",
    "mobx-react": "^6.1.4"
  }
}
